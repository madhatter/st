--- a/st.c
+++ b/st.c
@@ -851,8 +851,9 @@ void
 ttywrite(const char *s, size_t n, int may_echo)
 {
 	const char *next;
-	Arg arg = (Arg) { .i = term.scr };
-
-	kscrolldown(&arg);
+	if (may_echo) {
+		Arg arg = (Arg) { .i = term.scr };
+		kscrolldown(&arg);
+	}
 
 	if (may_echo && IS_SET(MODE_ECHO))
